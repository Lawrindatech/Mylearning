#include <stdio.h>
#include <math.h>

int main(){
    int num;
    double raised;
    printf("Enter number: ");
    scanf("%d", &num);
     printf("The answer is: %lf", sqrt(num));

     raised = sqrt(num);
     printf("The power of the number is: %lf", pow(num, s))
     return 0;

}